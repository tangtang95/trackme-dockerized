package com.trackme.trackmeapplication.request.groupRequest;

/**
 * The type of comparison between table fields available contains the filter statement
 *
 * @author Mattia Tibaldi
 */
public enum ComparisonSymbol {
    GREATER, LESS, EQUALS, NOT_EQUALS, LIKE
}
